# Copie para ".env.production" e preencha. Ver DEPLOY.md na raiz do repositório.
#
# O Frota entrou no grupo de login único em 2026-08-11 — deixou de ter conta e AUTH_SECRET
# próprios. DATABASE_URL, PORT, HOSTNAME, PASTA_ANEXOS e FORCA_HTTPS já vêm fixos no
# Dockerfile — não precisa repetir aqui.

# O MESMO valor precisa estar em portal, painel-locacao, rh, estoque, programacao,
# alojamentos, whatsapp, migracao-go e frota — é o segredo compartilhado que faz o login do
# Portal valer aqui (SSO). Comando sugerido no DEPLOY.md.
AUTH_SECRET="COLOQUE_O_SEGREDO_COMPARTILHADO_AQUI"
