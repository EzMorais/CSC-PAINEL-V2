# Variáveis usadas como build args do docker compose (ver DEPLOY.md) — só valores públicos
# (NEXT_PUBLIC_*), mas mesmo assim não precisa versionar a cópia de cada um.
.env
!.env.example

# Saídas locais do servidor Go
migracao-go/servidor.stdout.log
migracao-go/servidor.stderr.log

# Saída local de análise de dependências e código
graphify-out/
